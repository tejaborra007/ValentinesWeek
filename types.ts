
export interface ValentineDay {
  id: string;
  name: string;
  date: string;
  description: string;
  imageUrl: string;
  color: string;
  icon: string;
}

export interface LoveMessage {
  quote: string;
  reason: string;
  suggestion: string;
}
